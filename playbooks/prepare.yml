---
- name: Prepare Tart VMs
  hosts: all!localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Run prepare tasks from role
      ansible.builtin.include_role:
        name: tart_vm
        tasks_from: prepare.yml
