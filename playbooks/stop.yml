---
- name: Stop Tart VMs
  hosts: all:!localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Run stop tasks from role
      ansible.builtin.include_role:
        name: tart_vm
        tasks_from: stop.yml
