---
- name: Run VM
  ansible.builtin.command:
    argv: [ "tart", "run", "--no-graphics", "{{ vm.name }}" ]
  loop: "{{ vms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"
  async: 86400
  poll: 0
  tags:
    - deploy